# evaluation

Future: quality/performance metrics once there's a trained model worth measuring. `latency/`, `mos/` (mean opinion score), `wer/` (word error rate via ASR), `speaker/` (speaker similarity), `emotion/`, `codec/` (reconstruction quality — overlaps with `codec/evaluation/`, unreconciled, see `docs/roadmap.md`), `benchmark/`.
