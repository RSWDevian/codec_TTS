# preprocessing

Current: `text/` (normalize, tokenize), `audio/` (resample, normalize), `pipelines/` (composes them + Mimi encoding for dataset tokenization).

Future: `alignment/` (forced alignment for phoneme-level durations), `interleaving/` (text/audio token interleaving schemes for alternative backbone designs).
