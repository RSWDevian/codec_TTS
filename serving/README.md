# serving

Future: production-facing deployment of the inference pipeline. `api/` (REST), `websocket/`, `grpc/` — network protocols; `docker/`, `kubernetes/` — deployment manifests. Not needed for today's local prototype.
